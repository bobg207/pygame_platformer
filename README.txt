Concepts to cover

Settings file

# create a rectangle object for storing rectangular coordinates
pygame.draw.rec(display, color, (x, y, width, height), border)

# use the rectangles to check for collision
.colliderect()

jumping
falling
y_velocity change

get key press events
    pygame.key.get_pressed()